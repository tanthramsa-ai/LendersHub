# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Tenant root domain for host-based subdomains (e.g. acme.lendershub.in).
# Leave unset in local dev to fall back to path-based routing (localhost:3000/acme).
# NEXT_PUBLIC_TENANT_ROOT_DOMAIN=lendershub.in

# Database
DATABASE_URL=

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# API Keys (add as needed)
